apiVersion: v1
kind: Service
metadata:
    name: "devgovaks-97a8"
    labels:
        app: "devgovaks-97a8"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "devgovaks-97a8"